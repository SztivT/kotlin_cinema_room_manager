import java.util.*

fun main(args: Array<String>) {
    // write your code here
    val scanner = Scanner(System.`in`)
    println("${scanner.next()}:${scanner.next()}:${scanner.next()} ${scanner.next()}/${scanner.next()}/${scanner.next()}")
}